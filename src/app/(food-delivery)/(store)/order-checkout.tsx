import { View, Text } from 'react-native';
import React from 'react';
import CheckoutScreen from '@/screens/order/screens/order-checkout';

const OrderCheckout = () => {
  return <CheckoutScreen source="store" />;
};

export default OrderCheckout;
