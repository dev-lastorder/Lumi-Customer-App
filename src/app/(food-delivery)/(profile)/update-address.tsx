import { View, Text } from 'react-native';
import React from 'react';
import UpdateAddressScreen from '@/screens/profile/screens/update-address';

const UpdateAddress = () => {
  return <UpdateAddressScreen />;
};

export default UpdateAddress;
